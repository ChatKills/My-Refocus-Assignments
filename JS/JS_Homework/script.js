/** 
 * todo: create 2 variables using the "let" keyword:
 * @param firstname the parameter 
 * @param lastName
 * @param {string} variable
 * ! Warning
 * ? Question Mark
 * todo: Add sample values to the created variables.
 */

let firstName = "Darl Jed";
let lastName = "Matundan";

const msg = `Hello ${firstName} ${lastName}! How can we help you today?`

console.log(msg)