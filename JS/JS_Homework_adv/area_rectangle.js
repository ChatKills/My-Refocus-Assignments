/**
 * @param : width
 * @param : length
 * @param : area
 * todo : Calculate the area using the parameters width and length and output the result
 */ 

const length = 20;
const width = 16;
let area = length * width;

console.log(`Width: ${width}`);
console.log(`Length: ${length}`);
console.log(`Area : ${area}`);